`percollate md --individual --output tmp/ $article_urls` each of the following articles, then either generate a new AGENTS.md, critique the existing one, or update the existing one.
<articles>
- https://www.humanlayer.dev/blog/writing-a-good-claude-md
</articles>

If `percollate` is not installed, ask to run `npm -g i percollate`
